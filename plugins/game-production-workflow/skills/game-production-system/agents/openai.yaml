interface:
  display_name: "游戏生产协同系统"
  short_description: "从专业设计到可玩切片、验证、发布与交接"
  default_prompt: "Use $game-production-system to execute the next player-visible game slice from repository state, verify the real result, and leave a recoverable handoff."
