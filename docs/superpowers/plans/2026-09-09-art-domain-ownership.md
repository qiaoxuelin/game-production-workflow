# Art domain ownership implementation

Skill evolution id: art-domain-ownership
Skill evolution class: Restructure

1. Preserve baseline 4033dbe, the user correction, existing passing structure tests, and independent scenario observations. Validate marketplace/plugin identifiers.
2. Copy the visual-production contract into art first; add operation-aware 3D and animation/VFX requirements, preserving core authority and existing UI references. Change core and art discovery/routing consistently; replace the old core reference with a compatibility link only after the replacement exists.
3. Update active metadata, README, mechanical path/routing checks, and intended/boundary/off-route eval cases. Keep historical evidence untouched and existing context budgets unchanged.
4. Independently evaluate the candidate on the same raw scenarios. Address demonstrated errors, run required repository verification against 4033dbe and official skill/plugin validators, and inspect the full diff.
5. Refresh cachebuster, reinstall from the confirmed local marketplace, verify cache/source equality, commit and push the existing development branch, and update/read back the Feishu structure paragraph. Report evidence limits and new-thread pickup.

Validation outcomes and production-observation follow-up will be recorded with this plan after execution. No real game project is part of this maintenance task.

## Behavior validation

Independent baseline evaluator art_routing_baseline inspected five raw scenarios against 4033dbe before edits. It confirmed that 3D room Produce and character Review bypassed art; core offered generic spatial/runtime checks without explicit model/UV/topology/rig/deformation protocol. Fast material and non-art repairs remained direct; UI Design already worked.

Independent candidate evaluator art_routing_candidate inspected eight raw scenarios without design/plan/test expectations. All five baseline decisions retained appropriate proof and authority; 3D Produce/Review now route to art with conditional spatial/motion proof. Additional cases covered selected 3D Direction, 2D VFX and missing DCC/procedural fallback. It found two burden risks: 2D VFX could load UI production and static scene composition could inherit an interaction matrix. Both were corrected and re-evaluated read-only as resolved. This is simulated workflow evidence only; no 3D project or asset acceptance was performed.

Context checks: non-art Produce 5582 <= 5586; UI Design 9396 <= 9462; UI Produce 6714 < 6877. Existing ceilings unchanged. Seven new corpus cases cover the intended 3D/motion routes, Fast/static/noninteractive boundaries, non-art exclusion and missing capability. Art structure/corpus tests and official plugin/three-skill validation passed. Full repository regression and delivery verification follow on the committed candidate because regression fixtures clone HEAD.

## Delivery verification

Full `pwsh -NoProfile -File ./verify.ps1 -CompareRef 4033dbe` passed on committed candidate a33f7ff in an owned temporary clone made from a local Git bundle. Direct source execution hit the pre-existing sandbox-owner Git clone restriction; the isolated clone changed no product content and required no global trust change. The suite passed evolution policy, approval protocol/persistence, production contracts, art structure, convergence/Fast/v1.7 compatibility, behavior corpus, art fixtures, capability doctor, installer, and Linux/Windows workflow checks.

Official plugin and all three skill validators passed. The bundled Python lacked PyYAML; validation used PyYAML from a task-local temporary dependency directory.

Reinstalled `game-production-workflow@game-production-workflow`, version `1.8.0+codex.20260909084000`. All 83 plugin files match SHA-256 across source, verified clone, and installed cache (zero differences). New conversations pick up this skill version. The two real production observations remain pending as recorded in the design; workflow validation does not establish 3D art quality.
